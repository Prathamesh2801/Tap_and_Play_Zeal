# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Camera/WhatsApp masters and intermediate encodes. Hundreds of MB, and nothing
# in the build reads them — src/assets/video/*.mp4 is the committed output.
media-source/raw
media-source/encoded

# Build output zipped for hand-delivery to the Apache host. `npm run build`
# regenerates it; committing it once put 21 MB in the history.
dist.zip
*.zip

.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
